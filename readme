This bot notifies the user via Discord if it detects a rare NFT added to NFTX or Sudoswap.

Currently monitoring Forgotten Runes Warriors Guild, with future collections to be added

Use the command `nftx warrior` to call the bot in Discord 
